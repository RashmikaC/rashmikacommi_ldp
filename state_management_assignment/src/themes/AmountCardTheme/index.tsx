import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
      primary: {
        main: '#0052FF',
      },
    },
    typography: {
      button: {
        textTransform: "none",
        fontSize: "14px",
        letterSpacing: "0.01em",
      },
    },
  });