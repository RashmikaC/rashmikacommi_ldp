import {createTheme} from '@mui/material/styles';

 export const titleTheme= createTheme({
    typography:{
      subtitle1:{
        fontFamily:'sans-serif',
        fontSize:"1rem",
        fontWeight:700
      }
    },
  });