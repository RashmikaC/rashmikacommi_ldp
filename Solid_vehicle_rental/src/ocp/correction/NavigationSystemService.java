package ocp.correction;

public class NavigationSystemService extends OtherServices {
    public void provideService(boolean userChoice){
        if(userChoice)
        {
            // required code to add this service
        }
    }
}
